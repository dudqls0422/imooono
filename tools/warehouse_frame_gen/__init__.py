"""창고용 기본 프레임(그리드 + X방향 박공지붕) 생성기.

원본 ``창고설계용 기본 프레임 생성.py`` (532줄, Tkinter GUI)의 **검증된 순수
지오메트리 함수 5개를 바이트 단위 그대로 이식**하고, GUI/네트워크 결합
코드만 재구성했다. 원본 함수의 동작은 한 글자도 바꾸지 않았다 — 실제 동작은
``README.md`` "원본에서 확인된 실제 동작" 절 참조.

라이브 쓰기 미검증 경계: 빈 MIDAS 모델을 확보하지 못해 PUT 왕복은 미검증.
``midas-api`` catalog 스키마 기반이며 ``db/SWIND``·portal PR #9 전례처럼
catalog ↔ 라이브 불일치 가능.
"""

__version__ = "0.1.0"
