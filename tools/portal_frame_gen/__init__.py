"""포탈 프레임 모델 생성기.

Excel 템플릿의 형상/단면/재료 파라미터를 읽어 MIDAS Gen NX 빈 모델에
포탈(게이블) 프레임을 생성한다. 재료·단면은 **사전 정의 요구** — 도구는
기존 ``db/MATL`` / ``db/SECT`` 에서 이름으로 id 를 찾아 배정만 한다.

라이브 쓰기 미검증 경계: 이 패키지는 ``midas-api`` 스킬 catalog 스키마
기반 구현이며, 실제 MIDAS 쓰기 왕복은 미검증이다. QA 단계 또는 사용자가
빈 모델로 실행할 때 확인이 필요하다. ``db/SWIND`` 전례처럼 catalog ↔
라이브 불일치 가능성이 있다.
"""

__version__ = "0.1.0"
